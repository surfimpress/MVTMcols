# Pinned to versions that play together on macOS / Python 3.9+ as of
# 2026-04. torch ships an MPS-enabled wheel for Apple Silicon by default.
torch>=2.2,<3
torchvision>=0.17,<1
transformers>=4.38,<5
scikit-learn>=1.4,<2
Pillow>=10,<12
numpy>=1.26,<3
tqdm>=4.66,<5
